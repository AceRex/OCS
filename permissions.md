PERMISSION = ["presentation.basic"]
access: Media tab and features, Text tab and features, Scene tabs; add new scene modal/edit scene modal (Chorus flow should be locked, Repeat should be locked, sing along should be locked it should only be on manual, animation and transitions should be locked, user can only have one PPTX file at a time),

PERMISSION = ["timer.change_view"]
access: time view settings should be active

PERMISSION = ["timer.interval"]
access: set time interval should be active

PERMISSION = ["timer.start_time"]
access: start time should be active

Subscription tier = ["standard_setup", "large_setup", "premium setup"]
access: sessions feature should be active; when user sets time it should recording should start with the timer, bumper settings should also work, on starndard_setup users cannot edit pdf but on Large setup and premium setup users can edit pdf.

PERMISSION = ["scene.animations", "scene.transitions"]
access: the animation and transition should be active,

PERMISSION = ["song.repeat"]
access: the repeat song should be active

PERMISSION = ["sing_along"]
access: sing along should be active

PERMISSION = ["read_along"]
access: read along should be active

PERMISSION = ["song.chorus_flow"]
access: chorus flow should be active

PERMISSION = ["premium.full_access"]
access: full access should be active nothing should be locked

backend implementation:
2 months trial: 1 desktop/workstation and 3 mobile usage;
free mode:1 desktop/workstation and 3 mobile usage;
mini setup: 1 desktop/workstation and 5 mobile usage;
large setup: 2 desktop/workstation and 5 mobile usage;

if anyone is more than the assigned device logout the user automatically on all the devices
