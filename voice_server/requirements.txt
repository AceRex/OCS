# OCS Voice Sidecar — Python dependencies (v4 — Local Ollama AI)
# Install with: pip3 install -r requirements.txt
#
# Note: Ollama itself is a separate application — install from https://ollama.com
# Then pull models: ollama pull llama3.2 && ollama pull llava
faster-whisper
numpy
flask
scipy
pillow
requests
